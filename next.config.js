/** @type {import('next').NextConfig} */
const nextConfig = {
  // async redirects() {
  //     return [
  //       {
  //         source: '/',
  //         destination: '/auth/login',
  //         permanent: false, // Keep this as false for a non-permanent redirect
  //       },
  //     ];
  //   },
};

module.exports = nextConfig;
